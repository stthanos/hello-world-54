# yaml-language-server: $schema=https://cdn.raw-labs.com/endpoint-schemaV0.10.json
raw: "0.10"
endpoint: GET
metadata:
  title: Hello world studio 54
  description: Hello world example
  tags:
    - studio54
code: snapi
# declaration: hello_world
codeFile: let helloWorld = "Hello World";
format: json
security:
  public: true
computeClass: normal
enabled: true
tests:
  - name: Hello World test
    description: Tests a hello world endpoint
